import { Outlet } from "react-router-dom"

const AuthLayot =  ( ) => {

    return(
            <div className="container">
                <Outlet>
                    
                </Outlet>


            </div>

    )
}

export default AuthLayot;